{% extends 'BluesoftBundle::template.html.twig' %}

{% block body %}
    <h1>Contract edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}
    <a style="margin-top: 30px;" href="{{ path('index') }}" class="btn btn-primary">Powrót</a>
{% endblock %}
