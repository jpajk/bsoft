{% extends 'BluesoftBundle::template.html.twig' %}

{% block body %}
    <div class="row new-form-wrap col-xs-12 col-md-6">
        <h1>Contract creation</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
        {{ form_end(form) }}
    </div>

    <ul>
        <li>
            <a href="{{ path('contract_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
